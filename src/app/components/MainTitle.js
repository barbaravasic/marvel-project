import React from 'react';

const MainTitle = () => {
    return (
        <h2 className="main-title">
            Explore the Marvel Universe by searching for your favorite characters
        </h2>
    );
};

export default MainTitle;